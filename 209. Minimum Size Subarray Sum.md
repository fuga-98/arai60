# 進め方

Step1 : 問題を解く。

Step2 : 他の人のPRを参照し、コメントする。

Step3 : 3回続けてエラーが出ないように書く。ドキュメントを参照する。

# 実践

## Step1

### 思考ログ

i の更新を忘れた。

計算量は時間、空間ともにO(N)

実行時間は　10^5 / 10^6なので0.1秒くらい

```python
class Solution:
    def minSubArrayLen(self, target: int, nums: List[int]) -> int:
        min_length = math.inf
        prefix_sum = list(accumulate(nums))
        start = 0
        i = 0
        while i < len(nums):
            if prefix_sum[i] < target:
                i += 1
                continue
            while start < i:
                if prefix_sum[i] - prefix_sum[start] < target:
                    break
                start += 1
            min_length = min(min_length, i - start + 1)
            i += 1

        if min_length == math.inf:
            return 0
        return min_length

            
```

forで回したほうが自然に感じた

```python
class Solution:
    def minSubArrayLen(self, target: int, nums: List[int]) -> int:
        min_length = math.inf
        prefix_sum = list(accumulate(nums))
        start = 0
        for i in range(len(prefix_sum)):
            if prefix_sum[i] < target:
                continue
            while start < i:
                if prefix_sum[i] - prefix_sum[start] < target:
                    break
                start += 1
            min_length = min(min_length, i - start + 1)

        if min_length == math.inf:
            return 0
        return min_length

            
```

## Step2

### 同じ問題を解いた人のプルリクを見る

https://github.com/olsen-blue/Arai60/pull/50/files

- for で right 固定して、while で left を削るイメージ　同じ
- 番兵はどうするか。math.infで良いと感じてしまう。型違うけど、それはそれでおかしな実装をしたときに気づける気もする。
- 二分探索でも解ける。
    - イメージは駅の標高。単調増加なので、ロープウェイみたいなイメージか。標高の差がtarget以上場所を探す。
    - B駅　> A駅　+ targetであればTrue これだと一個目を０にしないと成り立たないですね。
    
    ```python
    class Solution:
        def minSubArrayLen(self, target: int, nums: List[int]) -> int:
            assert target >= 1
            if not nums:
                return 0
            prefix_sums = [0] + list(accumulate(nums))
            min_length = math.inf
    
            for start_index in range(len(prefix_sums)):
                target_sum = prefix_sums[start_index] + target
                end_index = bisect_left(prefix_sums, target_sum, lo=start_index + 1)
                if end_index == len(prefix_sums):
                    continue
                min_length = min(min_length, end_index - start_index)
            if min_length == math.inf:
                return 0
            return min_length
    ```
    

ノーミスで書けた。

分かりやすいのと、ずっと見ているので、olsenさんのソースは再現しやすいです。

https://github.com/hroc135/leetcode/pull/46/files

- 探索区間は前回の区間より右ではある。でも速度に影響はあまりないだろうとのこと。
- この程度の影響なら読む側の認知負荷を下げるためにloを設定しないほうがよさそう。

https://github.com/Ryotaro25/leetcode_first60/pull/53/files

- 一個の合計で出す方法。
- 累積和に飛びついてしまったが、これもあることは思いつきたい。

## Step3

### 3回連続で再現

二分探索

5mくら

```python
class Solution:
    def minSubArrayLen(self, target: int, nums: List[int]) -> int:
        if not nums:
            return 0
        if target <= 0:
            return 1
        
        min_length = math.inf
        prefix_sum = [0] + list(accumulate(nums))
        for base in range(len(prefix_sum)):
            desired = target + prefix_sum[base]
            index = bisect_left(prefix_sum, desired)
            if index == len(prefix_sum):
                continue
            min_length = min(min_length, index - base)
        if min_length == math.inf:
            return 0
        return min_length

```

線形

10m5m5m

```python
class Solution:
    def minSubArrayLen(self, target: int, nums: List[int]) -> int:
        if not nums:
            return 0
        if target <= 0:
            return 1
        
        min_length = math.inf
        prefix_sum = [0] + list(accumulate(nums))
        lo = 0
        for hi in range(len(prefix_sum)):
            while prefix_sum[hi] - prefix_sum[lo] >= target:
                min_length = min(min_length, hi - lo)
                lo += 1
        if min_length == math.inf:
            return 0
        return min_length
```

開区間を使うと処理がシンプルにかけるが、prefix_sumの最初を０にする必要があるのが少し気になるか。
