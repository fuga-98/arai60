# 進め方

Step1 : 問題を解く。

Step2 : 他の人のPRを参照し、コメントする。

Step3 : 3回続けてエラーが出ないように書く。ドキュメントを参照する。

# 実践

## Step1

### 思考ログ

候補をとってスタックかなんかに入れていって、確認してみるか。

wordをちぎっていくイメージ

スライスは使いたくないので、indexを進めて最後まで行けばOK

→結局wordsとの比較で回避できず。他に何かあるのかは2step以降で調べる。

**Time Limit Exceeded**

時間はだいたいwordの数×sの長さくらい？whileがどれくらいの計算量かわからない。

空間はStackのO(len(s)) 300

```python
class Solution:
    def wordBreak(self, s: str, wordDict: List[str]) -> bool:
        words = set(wordDict)
        max_length = max(len(word) for word in words)

        def proceed_indexes_if_match(start_index):
            # 次のIndexのを返す
            indexes = []
            for i in range(1, max_length + 1): #　+1をしていなかった
                if s[start_index:start_index+i] in words:
                    indexes.append(start_index + i)
            return indexes

        stack = proceed_indexes_if_match(0)
        while stack:
            index = stack.pop()
            if index == len(s): # ちぎり切った状態
                return True
            stack.extend(proceed_indexes_if_match(index))
        return False
```

BFSっぽいなと思ったので、

DFSっぽく書いてみる。一つ見つけたら良いので、こっちのほうが効率はよさそう。

```python
class Solution:
    def wordBreak(self, s: str, wordDict: List[str]) -> bool:
        words = set(wordDict)
        max_length = max(len(word) for word in words)
        break_positions = set()

        def search_indexes_if_match(start_index):
            # 次のIndexを返す
            for i in range(1, max_length + 1):
                break_index = start_index + i
                if break_index in break_positions:
                    continue
                if s[start_index:break_index] in words:
                    break_positions.add(break_index)
                    return start_index + i
            return None

        stack = [0]
        while stack:
            index = stack[-1]
            if index == len(s): # ちぎり切った状態
                return True
            next_index = search_indexes_if_match(index)
            if next_index is None:
                stack.pop()
                continue
            stack.append(next_index)
        return False
```

内海の勉強会で印象に残ったことが、

- Acceptされることを喜ばないほうが良い
- 書く能力よりも読む能力が足りていない。
- 脳内のデバッガーを走らせる

とおっしゃっていたので、そこを重点的に練習する。

自分の頭でソースを動かすことを考えながら書いたら、詰まらずにかけた

## Step2

### 同じ問題を解いた人のプルリクを見る

https://github.com/Mike0121/LeetCode/pull/52/files

- startswith
    - https://docs.python.org/ja/3.13/library/stdtypes.html#str.startswith
    - 実装
    - https://github.com/Exzrgs/LeetCode/pull/10#discussion_r1601378981
    - strのメソッドにはけっこうニッチそうなものもあることを認識しました。

https://github.com/olsen-blue/Arai60/pull/39

- 再帰のやり方。今回はDFSっぽさがあるので、しっくりくる。
- 後ろからちぎる

https://github.com/hroc135/leetcode/pull/37/files?short_path=c96a786#diff-c96a786e5eedab5279efc1d43a6b2f208b3276abbfb09af921260406a181d12d

- Trie、ローリングハッシュといった方法がある。
- 速度の考え方

```
現実的にはパフォーマンス不足がどういう感じになるかというと、たとえば、「一日一回深夜に30分で更新作業が走るようにしておく」などしてあるパイプラインが、だんだんデータとともに伸びていって「朝になっても更新が終わらないなあ」みたいになってきます。
あるいは「3日に1回くらいメモリー不足で更新プログラムが落ちる」みたいになってきます。

そうすると、手戻りが発生して、どこで問題が起きているかを漁ることになります。そして、見つけたら修正です。
だいたい、はじめにいわれたときの要件を守らない使われ方が気が付かないうちにされていて、だんだん耐えられなくなって崩壊するわけですね。

今回のこのコードは、想定される実行時間がそれほど長くなく、またメモリーも終了したら開放されるので、変なリスクをわざわざ取らないで単純で読みやすいほうに寄せたくなります。上のような事象を引き起こす可能性がとても小さそうだからです。
```

Trieはぎりぎり常識ではないが知っておいたほうがよいくらいのものらしいので調べる

[https://ja.wikipedia.org/wiki/トライ_(データ構造)](https://ja.wikipedia.org/wiki/%E3%83%88%E3%83%A9%E3%82%A4_(%E3%83%87%E3%83%BC%E3%82%BF%E6%A7%8B%E9%80%A0))

実装は重そうなのでパス

最近レビューしすぎて解くのとバランスが取れていない。

逆よりは良いのだろうけど、達成感があまりない

## Step3

### 3回連続で再現できるようになる

20分　4分　3分
```python
class Solution:
    def wordBreak(self, s: str, wordDict: List[str]) -> bool:
        checked = set()

        def break_word_if_match(word):
            for target in wordDict:
                if word.startswith(target):
                    breaked_word = word[len(target):]
                    if breaked_word in checked:
                        continue
                    checked.add(breaked_word)
                    return breaked_word
            return word
        
        stack = [s]
        while stack:
            word = stack[-1]
            breaked_word = break_word_if_match(word)
            if breaked_word == '':
                return True
            if breaked_word == word:
                stack.pop()
                continue
            stack.append(breaked_word)
        return False
```
