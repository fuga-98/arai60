# 進め方

Step1 : 問題を解く。

Step2 : 他の人のPRを参照し、コメントする。

Step3 : 3回続けてエラーが出ないように書く。ドキュメントを参照する。

# 実践

## Step1

### 思考ログ

まあ二分探索なんだろう。

sortされているものを探すときは二分探索が頭に浮かべばよいのだろうか。

単純にやるならfindを使えばよさそう。時間O(N)

[4.5,6,1,2,3]だとして、t =5なら

条件をnums[mid] < nums[-1]として、

FFFTTT

t >nums[-1] ならそこからFFFを二分探索すればよい。

t < nums[-1]ならTTTを二分探索すればよい

①素のソートされたListにもどして、チェックする

②FFFTTTを絞りこみながら探索していくとか

③ソートされた部分にわけて二分探索

①と③はほぼ同じで③のほうが実装がシンプルそう。

②は二つ条件があり、③より実装が煩雑になりそう。

```python
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        def search_break():
            lo = 0
            hi = len(nums) - 1
            while lo < hi:
                mid = (lo + hi) // 2
                if nums[mid] <= nums[-1]:
                    hi = mid
                else:
                    lo = mid + 1
            return hi
        
        break_point = search_break()
        if target <= nums[-1]:
            result = bisect_left(nums, target, lo=break_point, hi=len(nums))
            if result >= len(nums) or nums[result] != target:
                return -1
            return result 
        result = bisect_left(nums, target, lo=0, hi=break_point)
        if result >= break_point or nums[result] != target:
            return -1
        return result
```

-1と判定する条件で考慮漏れしてしまった。

②を自然言語で書く

targetはどちらの山に所属しているかチェックします。

山が異なっていたら違う山にいくよう範囲を狭めましょう。

山が一緒ならその山を二分探索しましょう。

分けたほうが分かりやすそうだと思いました。

## Step2

### 同じ問題を解いた人のプルリクを見る

https://github.com/olsen-blue/Arai60/pull/43/files

- bisectのkeyを用いた解法。面白い。けどこれは思いつけない。
- 一気に探索していくやり方は直すのは私は分岐が多くてミスりやすそうに見えます。

https://github.com/TORUS0818/leetcode/pull/45/files

- 二回探索派ですね。
- keyを用いた解法は勉強になりそう。

https://github.com/Mike0121/LeetCode/pull/45/files

- https://github.com/Mike0121/LeetCode/pull/45/files#r1739983112
    - 確かにその気持ちになりました。引数が多くなってくるとそう感じます。
- 計算量を考えるのを忘れていました。時間O(logn)空間は定数

```python
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        def generate(num):
            return (num <= nums[-1], target <= num) # num <= targetでWA
        result = bisect_left(nums, generate(target), key=generate)
        if result >= len(nums) or nums[result] != target:
            return -1
        return result 
```

具体例で考える

bisectの引数xは (FT) か（TT）になる。

FTのケースを考える

```python
    while lo < hi:
        mid = (lo + hi) // 2
        if key(a[mid]) < x:
            lo = mid + 1
        else:
            hi = mid
            
```

key(mid)は(FF)(FT)(TF)(TT)になり

Tから始まるならFalseでhi=midになる。

(FF)(FF)(FF)(FT)(FT)(FT)(TF)(TF)(TF)(TF)

みたいになるので、最小のFTを見つけるようにすればよい。

ライブラリを使うにしても、二分探索読めないと怖いですね。

## Step3

### 3回連続で再現できるようになる

if result >= len(nums) or nums[result] != target:
            return -1

これの最初の条件いらなそう

[0,1,2] t=3とすると、

[TF,TF, TF] t= FTとなるのでresult=0となります

key関数の命名がうまいものが思いつかないが、実務ならdocstringで説明を書くで十分な気がしている。

```python
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        def compare(num):
            return (num <= nums[-1], num >= target)
        result = bisect_left(nums, compare(target), key=compare)
        if nums[result] != target:
            return -1
        return result
```

keyが書けるのは3.10以降とのこと。けっこう新しい。
