# 進め方

Step1 : 問題を解く。

Step2 : 他の人のPRを参照し、コメントする。

Step3 : 3回続けてエラーが出ないように書く。ドキュメントを参照する。

# 実践

## Step1

### 思考ログ

最小値を求める問題。

これ勉強会でやっていたな。

前から見ていく方法はあるが、二分探索でやる。

nums[mid] > nums[mid+1]となる点を探せばよい。

端のケースだけ別で処理しないといけないかも。

いや、nums[x] < nums[0]となる最小のxを探すでもいけそう

nums[0]より大きければ右側 、nums[0]より小さければ左側か。

引継ぎのイメージがつく再帰で書いてみよう。

最小値xはleft ≤ x ≤ rightの中に。

```python
class Solution:
    def findMin(self, nums: List[int]) -> int:
        def find_min_helper(left, right):
            if left >= right:
                if nums[left] > nums[0]:
                    return nums[0]
                return nums[left]
            mid = (left + right) // 2
            if nums[mid] < nums[0]:
                # nums[mid]は最小になりうる
                return find_min_helper(left, mid)
            # nums[mid] > nums[0] のときmidは最小にならない。
            return find_min_helper(mid + 1, right)

        return find_min_helper(0, len(nums) - 1)
```

最初が最小値のときにうまく対応できない。

最小値が最初だと一番右にいくのでっていくので、判定をnums[-1]でやるとスムーズに。

```python
class Solution:
    def findMin(self, nums: List[int]) -> int:
        def find_min_helper(left, right):
            if left >= right:
                return nums[left]
            mid = (left + right) // 2
            if nums[mid] < nums[-1]:
                return find_min_helper(left, mid)
            return find_min_helper(mid + 1, right)

        return find_min_helper(0, len(nums) - 1)
```

入力を破壊してしまうけど、後ろにコピーをつくればいける。

```python
class Solution:
    def findMin(self, nums: List[int]) -> int:
        def find_min_helper(left, right):
            if left >= right:
                return nums[left]
            mid = (left + right) // 2
            if nums[mid] < nums[0]:
                return find_min_helper(left, mid)
            return find_min_helper(mid + 1, right)
        
        nums.append(nums[0])
        return find_min_helper(0, len(nums) - 1)
```

## Step2

開区間、閉区間の意味が分かった気がする。

left,rightが答えになりうるのかをはっきりさせましょう、ということか。

### 同じ問題を解いた人のプルリクを見る

https://github.com/olsen-blue/Arai60/pull/42/files

- 二分探索以外の方法でも解いている。
- 追加質問があるらしい。

https://github.com/Ryotaro25/leetcode_first60/pull/46

- 追加質問について考えてみる

https://github.com/philip82148/leetcode-arai60/pull/13

https://github.com/takuya576/leetcode/pull/2

レビューについて

> レビューにおいては、書かれていることを読み取って、書いた人の頭の中を推測し、どのような情報を伝えるとよりよくなるかを考えて、その情報を出すというのが理想的なプロセスです。
読み取れなかったならば、読み取れなかったので教えて欲しい、から入って、上のプロセスに合流します。
> 

> 今回、読み取れていないところで止まらず連想した情報をたくさん出していますが、分からなかったときに止まって確認に入らないのはエンジニアリング全般においてかなり悪い癖です。
> 

> 一般論として、「文字を書くこと」も「コストを増やす」行為であり、読んでもらった結果として「ベネフィットがコストを上回るか」のバランスで書くかを決めましょう。
> 

> 私は「何かを知っていることやできることそれ自体はいいことではない」と考えています。
> 

つまりレビューもエンジニアリングであるという意識を持ちましょうということですかね。

### 追加質問

このソースについて考える。

```python
class Solution:
    def findMin(self, nums: List[int]) -> int:
        def find_min_helper(left, right):
            if left >= right:
                return nums[left]
            mid = (left + right) // 2
            if nums[mid] < nums[-1]:
                return find_min_helper(left, mid)
            return find_min_helper(mid + 1, right)

        return find_min_helper(0, len(nums) - 1)
```

そもそもこれの気持ちを書くと、

探しているものは、nums[-1]より小さい最小の部分

（FFFFTTTTTの最初のT）

leftは探す必要のある最小

rightは探す必要のある最大

midが条件を満たすなら、答えになりうる最大はmidになるので、(left,mid)を引き継ぐ

midが条件を満たさないなら、mid+1以降に答えがあることがわかるので(mid+1, right)を引き継ぐ

1. 「2で割る処理がありますがこれは切り捨てでも切り上げでも構わないのでしょうか。」
2. 「nums[middle] <= nums[right] とありますが、これは < でもいいですか。」
3. 「nums[right] は、nums[-1] でもいいですか。」
4. 「right の初期値は nums.size() でもいいですか。」

１

そこまで変わらなそう。left,rightが隣あっているときは問題となりそうか。

ちゃんと引き継がれていればFTか、TT、FF（right=len(nums)-1のケース)になっていて

mid=rightになってしまい、mid=Tのとき、(left, right)で幅が狭くならない。

mid=Fの時も(right+1, right)で範囲外エラーを引き起こしそう。

実験結果

RecursionError: maximum recursion depth exceeded while calling a Python object
^^^^^^^^^^^^^^^^^^^^^^^^
mid = ceil((left + right) / 2)

やはり再帰制限ですね。エラーがここを指していてすごいなと思いました。

２

if nums[mid] < nums[-1]:　多分ここの等号を付けてよいかの話だと思う。

numsの要素はUniqueなので、nums[-1]のときにTになるくらいの影響のはず。

現状は最後の要素が最小の時はFFFFFとなるが、統合を付けるとFFFFTになるのでつけたほうが綺麗そう。

動いた

3

もとのソースはnums[mid] > nums[right]でやっていたみたいだ。

動く。

4

len(nums)でもよいかどうか。

これをするなら、引き継ぎの意味が変わるので動かないと思う。

rightは答えにならないことがわかっている最大の数になりますね。

right-1が答えになる可能性のある最大の数になりそう。

F?????TT

動かない。

条件を≤でやると動いた。

なるほど、FFFFだとout of rangeしてしまうが、FFFTだとTで探索が終わるので大丈夫なのか。

変更する、しないで動くものはどれかまで考えるのは時間がもったいない気がするので、一旦やらないでおきます。

## Step3

### 3回連続で再現できるようになる

考えて書けるようにこれまでとは違ったパターンでかいてみる

leftを答えにならない最小の数

rightを答えにならない最大の数とする。

終了条件はleft, x, right となればよいので、

left + 1 = right - 1なので

right - left = 2となるのとき

```python
class Solution:
    def findMin(self, nums: List[int]) -> int:
        left = -1
        right = len(nums)
        #終了条件は left + 1 == right - 1
        while right - left > 2:
            mid = (left + right) // 2
            if nums[mid] <= nums[-1]:
                right = mid + 1
            else:
                left = mid
        return nums[left + 1]
```

whileの条件を終了条件で書いてしまったが、それ以外はレバガチャしなかった。

```python
class Solution:
    def findMin(self, nums: List[int]) -> int:
        lo = -1
        hi = len(nums)
        while lo + 1 < hi - 1:
            mid = (lo + hi) // 2
            if nums[mid] <= nums[-1]:
                hi = mid + 1
            else:
                lo = mid
        return nums[lo + 1]
```

二分探索を感じられるようになってきました。
