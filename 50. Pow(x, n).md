# 進め方

Step1 : 問題を解く。

Step2 : 他の人のPRを参照し、コメントする。

Step3 : 3回続けてエラーが出ないように書く。ドキュメントを参照する。

# 実践

## Step1

### 思考ログ

まず素直に書いてみよう。

時間O(N)

```python
class Solution:
    def myPow(self, x: float, n: int) -> float:
        if n == 0:
            return 1

        result = 1
        if n > 0:
            for i in range(n):
                result *= x
        else:
            for i in range(-n):
                result /= x
        
        return result
```

x=0.00001, n=2147483647で時間オーバー

割り算は時間かかるということを聞いたことがあるな。

logとったらいけるか。

```python
class Solution:
    def myPow(self, x: float, n: int) -> float:
        if n == 0:
            return 1
        if x == 0:
            return 0
        if x > 0:
            log_x = log2(x)
            result_log = log_x * n
            result = pow(2, result_log)
        else:
            log_x = log2(-x)
            result_log = log_x * n
            result = pow(2, result_log)
            if n % 2 == 1:
                result *= -1

        return result
```

logから戻すときにpowを使っているけど、まあ後々良いやり方を見つけましょう。

## Step2

### 同じ問題を解いた人のプルリクを見る

https://github.com/olsen-blue/Arai60/pull/45/files

- 再帰、Bitでの計算
- 1.0 とすることで小数点が帰ることが分かりやすくなる。

https://github.com/fhiyo/leetcode/pull/46/files

- 再帰とループはループのほうが分かりやすいという人が多い。
- まあこの問題ならループだと思いました。
- Nodeとかは再起が分かりやすい気がします。

https://github.com/Yoshiki-Iwasa/Arai60/pull/38/files#r1711522299

- 小数点にびくびくするという話。
- 確かに。でもPythonならなんとかしているでしょ、みたいな気持ちがありました。
- ここらへんは後々調べよう

https://github.com/TORUS0818/leetcode/pull/47/files#r2038337006

n, xを破壊しない。

引数を破壊することは割と良くないことが多いですね。イミュータブルなオブジェクトでも。

なぜ早くなるかをGPTに質問

> 例: n = 13 (1101₂)
x¹   ← 下位ビットが 1 なので掛ける
x²   ← 0 なのでスキップ
x⁴   ← 1 なので掛ける
x⁸   ← 1 なので掛ける
最終結果 = x¹·x⁴·x⁸ = x¹³
> 
> 
> `cumulated` は **その時点の 2 乗累積 (x¹, x², x⁴, x⁸, …)** を保持
> 
> `n` を毎回 **右シフト** してビットを一つ消費
> 
> ビットが 1 のときだけ積に掛け合わせる
> 
> これで掛け算回数は **n のビット長 ≒ log₂ n** に抑えられます。
> 

うーん、まあ理屈は分かったが腹落ちはしてない。

## Step3

### 3回連続で再現できるようになる

```python
class Solution:
    def myPow(self, x: float, n: int) -> float:
        if n == 0:
            return 1.0
        if n > 0:
            base = x
        else:
            base = 1.0 / x
            n = -n
        
        bit = 1
        result = 1.0
        while bit <= n:
            if bit & n:
                result *= base
            base *= base
            bit <<= 1
        return result
```
bit演算なんか楽しいですね。
