# 進め方

Step1 : 問題を解く。

Step2 : 他の人のPRを参照し、コメントする。

Step3 : 3回続けてエラーが出ないように書く。ドキュメントを参照する。

# 実践

## Step1

### 思考ログ

算数でよく見た問題

DPで解けそう。時間、空間ともにO(N)  

N = 10,000 なので10msくらい

downとrightの並び替えの数を数えても良い。時間空間ともにO(1)

コンビネーションはライブラリがあるはず

https://docs.python.org/ja/3.13/library/math.html#math.comb

```python
class Solution:
    def uniquePaths(self, m: int, n: int) -> int:
        # 順列
        return math.comb(m+n-2, m-1)
```

```python
class Solution:
    def uniquePaths(self, m: int, n: int) -> int:
        # dp
        num_possible = [[0] * n] * m
        for row in range(m):
            for column in range(n):
                if row == 0 or column == 0:
                    num_possible[row][column] = 1
                    continue
                num_possible[row][column] = num_possible[row-1][column] + num_possible[row][column-1]
        return num_possible[m-1][n-1] # m,nと書いてしまった

```

## Step2

### 同じ問題を解いた人のプルリクを見る

https://github.com/saagchicken/coding_practice/pull/19/files

- 戻りは[-1][-1]で返すのがきれい
- forループを複数回回している、このやり方のほうが普通なのかな

https://github.com/olsen-blue/Arai60/pull/33/files

- 再帰の解法、直前の行のみ持っておく方法
- Cpythonを読む
    - step1で気になったので読みたい
    - https://github.com/olsen-blue/Arai60/pull/33/files#r1966730313
    

https://github.com/hroc135/leetcode/pull/31/files

- Pythonは気にしなくてもよいが、int型の最大値は気にする
    - https://github.com/hroc135/leetcode/pull/31/files#r1898544456
- 脳内デバッグの話。めんどくさがってしまうことがあるので、意識する
    - https://github.com/hroc135/leetcode/pull/31#discussion_r1898180689

## Cpythonのcombを読む

mathmodule.c.hとmathmodule.cがある

→ヘッダーファイルというものらしい。

Pythonでいうimportをまとめているイメージ？

[https://ja.wikipedia.org/wiki/ヘッダファイル](https://ja.wikipedia.org/wiki/%E3%83%98%E3%83%83%E3%83%80%E3%83%95%E3%82%A1%E3%82%A4%E3%83%AB)

読む側としては.cを見ればよさそう

探し方としては、メソッド名combをソース全体で検索して、それっぽいところを探すでよさそう？

math_comb → math_comb_impl → perm_comb　→ perm_comb_small

Cをあまり知らないので細かい値チェックは読み飛ばして大枠をつかむ。

perm_comb_small

```c
if (iscomb) {
    /* Maps k to the maximal n so that 2*k-1 <= n <= 127 and C(n, k)
     * fits into a uint64_t.  Exclude k = 1, because the second fast
     * path is faster for this case.*/
    static const unsigned char fast_comb_limits1[] = {
        0, 0, 127, 127, 127, 127, 127, 127,  // 0-7
        127, 127, 127, 127, 127, 127, 127, 127,  // 8-15
        116, 105, 97, 91, 86, 82, 78, 76,  // 16-23
        74, 72, 71, 70, 69, 68, 68, 67,  // 24-31
        67, 67, 67,  // 32-34
    };
    if (k < Py_ARRAY_LENGTH(fast_comb_limits1) && n <= fast_comb_limits1[k]) {
```

桁の計算が必要なのか。Pythonでは意識しないでいれているのとてもありがたい。

https://github.com/python/cpython/blob/619edb802e10741eff8eeab7dd279406bcd4b39c/Modules/mathmodule.c#L3551

- 奇数部分と偶数部分を分けてやるの面白い

https://github.com/python/cpython/blob/619edb802e10741eff8eeab7dd279406bcd4b39c/Modules/mathmodule.c#L3570

- スマートだけどわかりにくい。でもコメントがあるからわかる。

## Step3

### 3回連続で再現できるようになる

```python
class Solution:
    def uniquePaths(self, m: int, n: int) -> int:
        unique_paths_at_xy = [[0] * n] * m
        for y in range(m):
            for x in range(n):
                if x == 0 or y == 0:
                    unique_paths_at_xy[y][x] = 1
                    continue
                unique_paths_at_xy[y][x] = unique_paths_at_xy[y-1][x] + unique_paths_at_xy[y][x-1]
        return unique_paths_at_xy[-1][-1]
```

