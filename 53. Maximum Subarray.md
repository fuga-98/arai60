# 進め方

Step1 : 問題を解く。

Step2 : 他の人のPRを参照し、コメントする。

Step3 : 3回続けてエラーが出ないように書く。ドキュメントを参照する。

# 実践

## Step1

### 思考ログ

動的計画法だろう。

負の値しかないときは最大の値を返せばよいのか、０を返せばよいのか

手作業でやると、前の部分の最大値に自分の値を足す感じかな。前の値が負なら自分を入れる感じ

時間O(n) 空間O(n)

```python
class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        max_total_with_i = [0] * len(nums)
        max_total_with_i[0] = nums[0]
        for i in range(1, len(nums)):
            if max_total_with_i[i - 1] < 0:
                max_total_with_i[i] = nums[i]
                continue
            max_total_with_i[i] = max_total_with_i[i - 1] + nums[i]
        return max(max_total_with_i)
```

こうもかけそう

```python
class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        max_total_with_i = [num for num in nums]
        for i in range(1, len(max_total_with_i)):
            if max_total_with_i[i - 1] < 0:
                max_total_with_i[i] = nums[i]
                continue
            max_total_with_i[i] += max_total_with_i[i-1]
        return max(max_total_with_i)
```

## Step2

### 同じ問題を解いた人のプルリクを見る

https://github.com/olsen-blue/Arai60/pull/32/files

- ソースの感想を書いていて、記憶に残りやすそう
- Kadane’s algorismというらしい。常識ではないらしい。
    - 標高をイメージすると分かりやすい。
    - https://discord.com/channels/1084280443945353267/1206101582861697046/1207405733667410051
    - https://docs.google.com/document/d/11HV35ADPo9QxJOpJQ24FcZvtvioli770WWdZZDaLOfg/edit?tab=t.0#heading=h.bp0g0ai41eln

```python
class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        max_total = nums[0]
        min_total = 0 # nums[0] としてしまった
        prefix_total = 0
        for num in nums:
            prefix_total += num
            print(prefix_total)
            max_total = max(max_total, prefix_total - min_total)
            min_total = min(min_total, prefix_total)
        return max_total
        
        
```

[https://ja.wikipedia.org/wiki/累積和](https://ja.wikipedia.org/wiki/%E7%B4%AF%E7%A9%8D%E5%92%8C)

max_totalと最初に命令したので、prefix_totalと書いたが累積和はprefix_sumだった

https://github.com/TORUS0818/leetcode/pull/34

- DPってそうなんだ

> 現在の状態の値がひとつ以上前の状態の値によって決まるような問題を解くのに適しているのがDPで、各状態についての値を保存するために配列を用意するという方法がまずあり、工夫すれば配列を用意しないでも書けるくらいの話かなと思います
> 

https://github.com/hayashi-ay/leetcode/pull/36/files#diff-a6956948eb5bc3137495f6d70424f00900c8ba03395ebacccf937a76b9a4d988R60

- Devide and Conquerでも解ける
- 左側と右側と真ん中に分割して、左側の最大、右側の最大、中央をまたぐ最大をだしてその最大値を使う。
- あんまり直観的ではない感じがする。中央をまたぐ最大を出すならそのまま答えだしたらよいじゃん、って思う。

## Step3

### 3回連続で再現できるようになる

```python
class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        prefix_sum = 0
        max_prefix_sum = -math.inf
        min_prefix_sum = 0
        for num in nums:
            prefix_sum += num
            max_prefix_sum = max(max_prefix_sum, prefix_sum - min_prefix_sum)
            min_prefix_sum = min(min_prefix_sum, prefix_sum)
        return max_prefix_sum
```

 max_ending_here = prefix_sum - min_prefix_sum と変数に直したほうが分かりやすいかも

```python
class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        prefix_sum = 0
        max_prefix_sum = -math.inf
        min_prefix_sum = 0
        for num in nums:
            prefix_sum += num
            max_prefix_sum
            max_ending_here = prefix_sum - min_prefix_sum
            max_prefix_sum = max(max_prefix_sum, max_ending_here)
            min_prefix_sum = min(min_prefix_sum, prefix_sum)
        return max_prefix_sum
```
